
lib_src_files += files('ass_parser_lib.h')

lib_inc_dirs += include_directories('.')

install_headers(
    files('ass_parser_lib.h'),
    install_dir: install_include_dir / 'public',
    preserve_path: true,
)
