
lib_src_files += files('helper.c', 'helper.h', 'parser.c', 'parser.h', 'warnings.c', 'warnings.h')

install_headers(
    files('parser.h', 'warnings.h'),
    install_dir: install_include_dir / 'parser',
    preserve_path: true,
)
