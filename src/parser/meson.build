
lib_src_files += files('helper.c', 'helper.h', 'parser.c', 'parser.h', 'diagnostics.c', 'diagnostics.h')

install_headers(
    files('parser.h', 'diagnostics.h'),
    install_dir: install_include_dir / 'parser',
    preserve_path: true,
)
